/**
 * Application-wide constants
 */

/**
 * Maximum number of restaurants to show per group session
 * Prevents users from swiping through hundreds of places
 * and ensures consistent experience across all group members
 */
export const RESTAURANT_LIMIT = 20;

/**
 * Group expiration time in hours
 */
export const GROUP_EXPIRATION_HOURS = 24;

/**
 * Minimum members required to start swiping
 */
export const MIN_MEMBERS_TO_START = 2;
